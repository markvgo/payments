import { DbDirective } from './db.directive';

describe('DbDirective', () => {
  it('should create an instance', () => {
    const directive = new DbDirective();
    expect(directive).toBeTruthy();
  });
});
